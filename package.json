{
  "name": "ycg-meteo",
  "version": "1.0.0",
  "scripts": {
    "dev": "babel src -d dev",
    "build": "BABEL_ENV=production babel src -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-minify": "^0.5.0"
  }
}
